export type Airport = {
  code: string;
  id: string;
  lat: string;
  lng: string;
  name: string;
};
export type Flight = {
  code_departure: string;
  code_arrival: string;
  price: string;
  transfers: string;
};
